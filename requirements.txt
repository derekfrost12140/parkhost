Flask==2.0.1
flask-cors
opencv-python-headless==4.5.5.62
torch==2.2.0  # Specify the version if possible to avoid compatibility issues
torchvision==0.8.2  # As required by your YOLO version, if applicable
python==3.12.3
